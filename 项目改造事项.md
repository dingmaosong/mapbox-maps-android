# 字体本地化操作指南
## 组件内需要完成动作
1. [download_glyphs.py](tools/fonts/download_glyphs_Open Sans Regular.py) 替换里面的token成自己的
2. 执行：mkdir -p ~/mapbox-glyphs
        cd ~/mapbox-glyphs
3. 拷贝[download_glyphs.py](tools/fonts/download_glyphs_Open Sans Regular.py) 到新建好的 mapbox-glyphs 目录下面
4. 执行：python download_glyphs.py
5. 观察执行日志为：✅ 所有 glyph 下载完成 即为全部下载成功，可以参考[downLoad_glyphs_log.txt](tools/fonts/downLoad_glyphs_Open Sans Regular_log.txt)、
6. 检查下glyphs目录下glyphs文件，如果文件数量为0，则下载失败，请重新下载，正常数量为256个文件
7. 将下载好的glyphs目录拷贝到 [maps-sdk](maps-sdk) 的 [assets](maps-sdk/src/main/assets) 目录下，命名为 [OpenSans-Regular](maps-sdk/src/main/assets/glyphs/OpenSans-Regular)
8. [offline.json](maps-sdk/src/main/assets/styles/offline.json) 增加离线 glyphs：["glyphs": "asset://glyphs/{fontstack}/{range}.pbf",]
9. 先使用自定义的离线样式 [test.json](maps-sdk/src/main/assets/styles/test.json) 修改 [Style.kt](maps-sdk/src/main/java/com/mapbox/maps/Style.kt)中的[OFFLINE]指向本地[styles](maps-sdk/src/main/assets/styles)中的test.json，找到CustomTileSourceActivity中的[验证test.json]注释代码，编译运行后能看到地图上有 [XAG] 字体
10. 全局搜索：fonts/v1 和 mapbox://fonts 以及 api.mapbox.com/fonts ✅ 它们在 Native C++ 核心（mapbox-gl-native / mapbox-maps-core）里
11. 到现在为止，java层的网络请求已经不会触发，接下来处理 “字体下载逻辑在 Native Core，不在 SDK Java 层”，“在 SDK 层拦截 / 重定向 Native Core 的 glyph 请求”
12. 既然 Native Core 一定会发 glyph 请求 那我们就在 FileSource 层 拦截： 如果 URL 是 glyph 直接从 assets/glyphs 读 不再交给网络栈 📌 这是“私有化 / 离线合规”的标准做法
13. fork[[MapInitOptions.kt](maps-sdk/src/main/java/com/mapbox/maps/MapInitOptions.kt)]初始化代码
14. 到目前为止资源不会触发网络请求
15. 接下来出来event.mapbox.com，这里需要移除掉[module-telemetry](module-telemetry) 并解决编译问题
16. 接下来又发现config.mapbox.com有访问问题，需要继续寻找原因

## app需要完动作
1. 禁止使用类似：这是在线资源，请使用本地资源
   1) mapboxMap.loadStyleUri(Style.MAPBOX_STREETS)
   2) mapboxMap.loadStyle(Style.MAPBOX_STREETS)
   3) 使用方式改为：[mapboxMap.loadStyle(Style.OFFLINE)]， 调用[Style.kt](maps-sdk/src/main/java/com/mapbox/maps/Style.kt)中的 [OFFLINE] 指向本地 [styles](maps-sdk/src/main/assets/styles)
   4) 或者改为：[mapboxMap.loadStyleJson(styleJsonString)]
2. 禁止使用类似：这是在线资源，请使用本地资源
