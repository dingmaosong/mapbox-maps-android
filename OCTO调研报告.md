# Mapbox 加载 OctoMap（.ot）三维数据的技术调研报告

---

## 1. 调研背景与目标

### 业务背景
- 航测、建筑三维重建、无人机/飞行机器人作业可视化需要快速展示空间占据信息
- 数据来源以点云/体素为主，传统 Mesh 管线成本高、前处理重

### 当前问题
- 现有业务需要在移动端或轻量端快速完成三维可视化验证
- 常见 3D 引擎对 OctoMap 原生支持不足，需定制解析与渲染路径

### 本次调研目标
- ✔ 验证 Mapbox + OctoMap 的工程可落地性
- ✔ 明确系统架构与数据流转路径
- ✔ 识别性能瓶颈、适配范围与扩展方向

---

## 2. OctoMap 数据简介

### OctoMap 基本概念
- OctoMap 是八叉树结构的体素地图，用于空间占据表达
- 叶子节点表示空间中可占据体素，适合稀疏空间表达

### `.ot` 文件特点
- OctoMap 标准序列化格式
- 支持 ColorOcTree（带颜色）与 OcTree（无颜色）
- 以树结构保存，占据体素可直接遍历

### 与常见三维格式对比

| 格式 | 数据组织 | 主要用途 | 解析成本 | 适合场景 |
|---|---|---|---|---|
| OctoMap (.ot) | 八叉树体素 | 占据/稀疏空间 | 中等 | 航测、机器人、建图 |
| PLY/PCD | 点云 | 原始采样 | 低 | 点云可视化 |
| OBJ/FBX | Mesh | 渲染展示 | 高 | 高保真模型 |
| 3D Tiles | 分块结构 | 大规模城市 | 高 | 城市级渲染 |

---

## 3. 整体技术方案设计

### 技术选型说明
- Mapbox：提供稳定地图底图、相机控制与自定义层扩展能力
- OctoMap：直接加载体素结构，避免 Mesh 化处理
- OpenGL 点云渲染：轻量、可控、满足 PoC 验证

✔ 选型理由：以最小工程成本完成“空间体素 + 地图叠加”的三维可视化验证

### 系统整体架构

```
[.ot 文件 Assets]
      |
      v
[JNI OctoMapLoader]
      |
      v
[octomap_renderer.cpp]
      |
      +--> voxelBuffer (x,y,z,r,g,b) --> VoxelMeshBuilder --> GLRenderer (点云)
      |
      +--> vertexBuffer (mercator x,y,z) --> CustomLayer --> Mapbox
```

### 数据流转关系（示意图）

```
Assets(.ot)
  |
  v
JNI 解析 -> Octree 遍历 -> 体素坐标 + 颜色
  |
  +--> Kotlin 侧构建点云 -> OpenGL 渲染
  |
  +--> Mercator 顶点 -> Mapbox CustomLayer
```

---

## 4. OctoMap 数据解析方案

### `.ot` 文件加载思路
- 通过 AssetManager 读取文件缓冲区
- 使用 OctoMap 的 AbstractOcTree::read 反序列化
- 支持 ColorOcTree 与 OcTree 两类树

### Octree 数据处理方式
- 遍历 leafs
- 过滤 occupied 体素
- 将 ENU 坐标与颜色写入 voxelBuffer

✔ 已验证：可正确解析并生成体素点云

### 坐标系与空间映射说明
- OctoMap 以 ENU 为本地坐标
- 使用 setOrigin 设置地理原点（WGS84）
- 转换为 Mercator 用于 Mapbox 叠加

---

## 5. Mapbox 三维渲染实现方案

### 使用的 Mapbox 能力
- CustomLayer：叠加自定义渲染内容
- 相机同步：保持点云与地图视角一致

### 三维渲染方式说明
- OpenGL 绘制点云
- 采用 interleaved buffer 存储 position + color
- 渲染与 Mapbox 底图叠加

✔ 已验证：点云在地图坐标中稳定显示

### 性能与渲染策略
- 当前策略：全量体素渲染
- ⚠ 当点数增大，需引入分块与裁剪

---

## 6. 相机与视角自适应设计

### 如何从 `.ot` 数据计算初始视角
- 解析 min/max bounds 得到数据范围
- 计算中心点与合适 zoom

### 自动设置中心点与缩放级别
- 以 bounds 中心作为地图中心
- 根据范围计算 zoom

### 交互体验说明
- 初始加载后自动定位到数据范围
- 支持地图拖拽、缩放与旋转

✔ 已验证：无需手动定位，自动完成初始视角适配

---

## 7. Demo 实现与验证结果

### Demo 能力说明
- 加载 assets 内 .ot 文件
- 解析为体素点云并渲染
- Mapbox 相机同步与交互支持

### 已验证的关键技术点
- ✔ `.ot` 文件解析可用
- ✔ ENU → Mercator 映射可用
- ✔ 自定义 OpenGL 点云可叠加渲染
- ✔ 自动相机定位可用

### 可插入截图的位置说明
- 建议插入：加载成功视角截图（含地图底图 + 点云）

---

## 8. 方案优势与局限性分析

### 优势总结
- ✔ 轻量、工程成本低
- ✔ 可直接复用 OctoMap 数据，无需 Mesh 化
- ✔ 与地图底图结合适合航测/机器人场景

### 当前限制
- ⚠ 点云规模过大易导致渲染性能下降
- ⚠ JNI 传输大数组存在内存压力
- ⚠ 缺乏 LOD 与分块机制

### 潜在风险
- ⚠ 体素密度极不均导致局部爆点
- ⚠ 坐标原点配置错误会导致位置偏移

---

## 9. 可扩展方向与后续规划

### 技术演进方向
- ➜ 分块加载与 LOD
- ➜ 视锥/距离裁剪
- ➜ GPU 缓存与淘汰策略

### 与无人机 / 机器人系统结合点
- ➜ 实时更新增量 OctoMap
- ➜ 多传感器融合结果直接可视化
- ➜ 可与路径规划、任务分配系统联动

---

## 10. 结论

✔ Mapbox + OctoMap 的组合已完成 Demo 验证，可用于中小规模体素数据展示  
⚠ 当数据规模上升，需要引入分块、裁剪与增量更新机制  
➜ 方案具备工程落地性，适合作为无人机/机器人场景的空间可视化基础架构